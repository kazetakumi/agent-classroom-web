---
id: US-016
feat: FEAT-003
title: Results screen rewrite
status: todo
blocked: [US-011]
---

## What to build

Rewrite `ResultsScreen.tsx` to the C-Ledger aesthetic. IBM Plex Mono receipt-style table showing Correct / Wrong / Skipped counts, Score, and Time using CSS classes `ledger-label`, `ledger-value`, `ledger-value-dim`, and `ledger-rule`. A `flex: 1` spacer pushes Sage's closing comment to the bottom of the content area. Below the data: a "Your move" break section with a "Review answers →" button. `SageTrigger` with `showNav={false}` (Sage strip only, no nav row) anchored at the bottom. Component scoped to `.results-screen` CSS namespace.

## Acceptance criteria

- [ ] Ledger table renders Correct, Wrong, Skipped, Score, and Time rows
- [ ] Ledger uses IBM Plex Mono with correct label/value/dim/rule CSS classes
- [ ] Sage closing comment is rendered below the data
- [ ] "Review answers →" button is present and triggers navigation to Review Grid
- [ ] `SageTrigger` renders with `showNav={false}` (no nav row in DOM)
- [ ] Component is scoped under `.results-screen` CSS namespace

## Tests

- All five ledger rows (Correct, Wrong, Skipped, Score, Time) are present in the DOM
- `SageTrigger` rendered with `showNav={false}` — nav row absent
- "Review answers →" click calls the `onReview` prop

## Blocked by

US-011
